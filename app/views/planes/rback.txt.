<!--
<h1>Planes</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Description</th>
      <th>Datetime</th>
      <th>Longitude</th>
      <th>Latitude</th>
      <th>Elevation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @planes.each do |plane| %>
      <tr>
        <td><%= plane.id %></td>
        <td><%= plane.description %></td>
        <td><%= plane.datetime %></td>
        <td><%= plane.longitude %></td>
        <td><%= plane.latitude %></td>
        <td><%= plane.elevation %></td>
        <td><%= link_to 'Show', plane %></td>
        <td><%= link_to 'Edit', edit_plane_path(plane) %></td>
        <td><%= link_to 'Destroy', plane, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Plane', new_plane_path %> -->
